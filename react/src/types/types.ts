export type LoginType = {
    email: string;
    password: string;
};

export type SignUpType = {
    firstname: string;
    lastname: string;
    email: string;
    password: string;
    passwordConfirm: string;
};
